import { HostelType } from '@/types/common.types';

export const HOSTELS: HostelType[] = ['Q2', 'Q2.0', 'Q2.1'];

export const DEFAULT_HOSTEL: HostelType = 'Q2';

export const DEFAULT_LATE_FEE_PER_DAY = 20;

export const DEFAULT_GRACE_PERIOD_DAYS = 5;
